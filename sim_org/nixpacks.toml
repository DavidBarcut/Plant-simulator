[phases.setup]
nixPkgs = [
  "python311",
  "xvfb",
  "libsdl2",
  "libsdl2-image",
  "libsdl2-mixer",
  "libsdl2-ttf",
  "fontconfig"
]


[start]
cmd = "xvfb-run -a -s '-screen 0 1024x768x24' gunicorn -w 1 -k eventlet -b 0.0.0.0:$PORT app:app"


